@model IEnumerable<NCSafety.Models.Lab>

@{
    ViewBag.Title = "Index";
}
<br />
<h2>Labs</h2>
<br />
<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.labBuilding)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.labNumber)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.schID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.labBuilding)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.labNumber)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.schID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
